# Raw Data Inputs for USDA Nationwide Build

Place raw source files here before running the build script.

Expected files:
- `zip_reference_us.csv`
- `food_access_atlas.csv`
- `zip_tract_crosswalk.csv`
- `snap_retailers_raw.csv`

Then run:

```bash
python scripts/build_usda_datasets.py
```
